//https://www.youtube.com/watch?v=yBFHwJgqLD4
//clouseure
const debounce = (fn, delay) => {
  let id;
  return (...args) => {

    //settimeout 
    //we will call the function that was passed into begin with at the top 
  }
}
//避免按鈕被重複點擊多次
